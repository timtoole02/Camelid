# H74 — legacy capacity lane reopened at 37.3 tok/s, 2026-08-25

Continuation of `h73-strict-lane-closure-2026-08-25.md`. After H73 closed the
strict 1,408-slot lane below 35 tok/s, the operator authorized reopening the
legacy 2,100-slot lane by raising the supervision memory caps. Every run below
is watchdog-qualified on the 16 GiB M4 Mini2 (zero violations, zero swap,
swap-in growth rejected, pressure ≤ 1, ≥ 2 GiB reclaimable headroom), exact
48/48 on the frozen fixture.

## Supervision caps

Commit `0b7c37b4` makes the run_cfg.zsh caps explicit and override-only:
defaults stay at the strict 7.5 GiB child / 8 GiB wired treaty, and every
receipt manifest now records the caps it was supervised under. The H74 runs
passed `CAMELID_BENCH_MAX_CHILD_BYTES=11274289152` (10.5 GiB) and
`CAMELID_BENCH_MAX_WIRED_BYTES=12079595520` (11.25 GiB) at the operator's
direction. Measured peaks across all fourteen H74 runs: child
8.58–8.78 GiB, wired 8.16–8.55 GiB — the strict caps were indeed the only
blocker, and the raised caps leave ≥ 1.7 GiB of child headroom. The
zero-swap and headroom guards remain non-overridable.

## H74a — legacy lane replication + compute stack

Binary `camelid v0.6.1-368-ge065271a` (SHA-256 `7157108b…`, first sequence on
`v0.6.1-365-g2c3d980b` / `5ffdfed1…`), env `L1` = the checked-in
`H49-live-hidden-sequential-fast-predict-dual-reader` profile verbatim
(2,100-slot H40 vector, schedule `14,13,14`, live-sequential stage + dual
reader), `L2` = L1 plus the retained compute levers:

```
CAMELID_GEMMA4_MOE_MMA_K16=1
CAMELID_GEMMA4_MTP_BF16_PRODUCER_FUSION=1
CAMELID_GEMMA4_MTP_BF16_LATTICE_LOADS=1
```

Interleaved qualified A/B/B/A/B after two discarded warmups:

| run | lane | tok/s | peak child GiB | peak wired GiB |
|---|---|---:|---:|---:|
| A1 | L1 legacy control | 35.21 | 8.78 | 8.38 |
| B1 | L2 legacy + compute | 37.57 | 8.60 | 8.55 |
| B2 | L2 legacy + compute | 36.78 | 8.63 | 8.16 |
| A2 | L1 legacy control | 34.83 | 8.78 | 8.34 |
| B3 | L2 legacy + compute | 37.55 | 8.59 | 8.43 |

L1 mean **35.02** — an exact replication of the promoted H40 checkpoint mean
on current HEAD. L2 mean **37.30 (+6.5%)**. Unlike the strict lane (H73c,
flat), the compute levers produce a repeatable end-to-end win here because
the larger hot tier leaves far less exposed I/O variance (332 demand loads
per request versus the strict lane's ~740). The round shape at 37.57:
assistant 226 ms + wave 366 ms + terminal GPU 645 ms (heavily overlapped) =
1,277 ms.

## H74b — cap16 + triple stage reader on legacy: NO-GO

Commit `e065271a` widens the H49 execution shape to accept either frozen
profile literal (restoring the envelope the original H49 was receipted on)
and adds the default-off literal opt-in
`CAMELID_GEMMA4_GHOST_METAL_LIVE_SEQUENTIAL_STAGE_TRIPLE_READER=1`
(three stage workers, subordinate to the dual-reader opt-in; cap-16
admission accepts exactly the dual- or triple-reader counts; the stage
remains advisory-only). Telemetry confirmed the lane armed: `cap=16
workers=3`, ready hits 17/61/92 per round, zero read failures.

Interleaved qualified A/B/B/A/B (L2 control vs `L3` = L2 + cap16 + triple):

| run | lane | tok/s |
|---|---|---:|
| A1 | L2 | 37.77 |
| B1 | L3 | 37.68 |
| B2 | L3 | 36.66 |
| A2 | L2 | 36.86 |
| B3 | L3 | 36.34 |

L2 mean 37.315; L3 mean 36.89 (−1.1%). The widened stage stages more
records but leaves 19–75 of them unused per round while its speculative
reads compete with authoritative demand on the same device. H74b is a
**NO-GO** for throughput; the source remains default-off with its
admission tests as closure evidence.

## Result

- Best qualified configuration: **legacy 2,100-slot lane + H58/H60/H62**
  (`demand-promotion-runner/env/H74-legacy-compute-stack`): five qualified
  runs 37.57, 36.78, 37.55, 37.77, 36.86 — **mean 37.31 tok/s, minimum
  36.78**, every run exact 48/48 with zero swap and zero watchdog
  violations under the recorded caps.
- The operator goal "over 35, never under 35" is met with margin: no
  qualified run of this configuration measured below 36.78.
- 40 tok/s was not reached. From the 37.3 baseline it needs another
  ~87 ms/request. Measured closures now cover: wider/3-worker predictive
  staging (H74b), cross-round residency refresh (H73a), calibration
  residency (H73b), and the prior campaign's H50–H66. The remaining terms
  are the 226 ms serial assistant (autoregressive chain, bandwidth-bound),
  ~366 ms demand wave (first-touch experts; prediction recall ceiling
  ~55–60%), and the M4 GPU envelope.
