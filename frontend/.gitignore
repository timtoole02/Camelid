# Personal cluster bootstrap from the removed Cluster Topology page. Nothing reads it any more,
# but a leftover local copy holds real machine addresses, so it must still never be committed.
public/cluster-import.json
