{
 "mode": "allowed",
 "routing": "Routing\nAlso placing on other engines\nWhat this proxy is accepting now\npublishes no load to rank on\n\nranked as if already carrying 2 requests, because it reports none\n\nb-ollama\nollama 0.33.2\nthe API has no queue-depth or capacity endpoint\nfrom the API\nc-ollama2\nollama 0.33.2\nthe API has no queue-depth or capacity endpoint\nfrom the API\na full queue is indistinguishable from a failure\n\nany refusal it sends is relayed to the client once and never retried on another node\n\nb-ollama\nollama 0.33.2\nno typed queue-full refusal, so a busy node cannot be told from a broken one\nfrom the API\nc-ollama2\nollama 0.33.2\nno typed queue-full refusal, so a busy node cannot be told from a broken one\nfrom the API\ncannot attest a warm prefix, so affinity would be a guess\n\na request pinned to it with x-camelid-fabric-sticky is refused rather than silently re-routed\n\nb-ollama\nollama 0.33.2\n`/api/ps` says which models are resident, not whether a session's prefix survived\nfrom the API\nc-ollama2\nollama 0.33.2\n`/api/ps` says which models are resident, not whether a session's prefix survived\nfrom the API\nb-ollama requests carrying tools are never placed here: tool calls have not been measured on ollama 0.33.2\nc-ollama2 requests carrying tools are never placed here: tool calls have not been measured on ollama 0.33.2\nb-ollama rerank requests are never placed here: its API has no rerank route\nc-ollama2 rerank requests are never placed here: its API has no rerank route\nAdded to the nodes file since this proxy started\nc-ollama2 (ollama)\n\nTo place on Camelid engines only again:\n\nRestart the proxy with this, keeping the other flags it was started with. Leave out --allow-mixed-engines. This page cannot change a running proxy.\n\ncamelid fabric serve …\nCopy",
 "row": "b-ollama\nollama\nrouted to · accepting 3 limits\nready\n127.0.0.1:11434\nllama32-1b-q8-r1:latest\nunknown\n2 ms",
 "drawer": "b-ollama\nready\nAddress\n127.0.0.1:11434\nEngine\nollama\nThis proxy sends work here, accepting what it was told to accept:\npublishes no load to rank on — ranked as if already carrying 2 requests, because it reports none\na full queue is indistinguishable from a failure — any refusal it sends is relayed to the client once and never retried on another node\ncannot attest a warm prefix, so affinity would be a guess — a request pinned to it with x-camelid-fabric-sticky is refused rather than silently re-routed\nActive model\nllama32-1b-q8-r1:latest\nLoaded models\nllama32-1b-q8-r1:latest\nExecution backend\nunknown\nNode build\n0.33.2\nLoad\nunknown\nProbe round-trip\n2 ms one sample; routing does not rank on it\nAs of\n2:12:19 PM\n\nWhen this page last read the proxy. The proxy does not publish when it last probed the node.\n\nWhat this engine can be asked\nload reporting\nno\nfrom the API\ntyped backpressure\nno\nfrom the API\nwarm prefix\nno\nfrom the API\ntool calls\nunknown\nnot checked\nembeddings\nyes\nfrom the API\nhistory neutral\nno\nmeasured here\nloads on demand\nyes\nfrom the API\nrerank route\nno\nfrom the API\n\nA measurement is about the exact build it was taken on, so a neighbouring version inherits nothing from it.\n\nAsk this node directly — useful when the proxy cannot reach it but you can.\n\nhttp://127.0.0.1:11434/v1/health\nCopy health URL",
 "requests": [
  "GET http://127.0.0.1:4175/v1/health",
  "GET http://127.0.0.1:4175/__camelid/backend/status",
  "GET http://127.0.0.1:4175/v1/models",
  "GET http://127.0.0.1:4175/api/capabilities",
  "GET http://127.0.0.1:4175/api/models/catalog/downloads",
  "GET http://127.0.0.1:4175/api/models/local",
  "GET http://127.0.0.1:4175/api/models/current",
  "GET http://127.0.0.1:8282/v1/health"
 ],
 "errors": []
}